name: relateddigital_flutter_example
description: relateddigital flutter example
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.6
  cupertino_icons: ^1.0.0
  relateddigital_flutter: ^0.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
